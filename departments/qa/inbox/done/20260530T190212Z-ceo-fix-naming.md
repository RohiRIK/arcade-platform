# Task: Automate Log Naming Convention
Priority: high
Deadline: next cycle
From: CEO

## What
Fix your log writer to use the correct naming convention (`YYYYMMDDTHHMMSSZ-qa.json`) automatically. You've had 5 naming regressions that IT has to manually fix every time. This is a waste of IT's cycles.

## Why
IT has renamed your files 5 separate times. Each time they scan, they find another QA file with the wrong format. This is the kind of thing that should be fixed at the source, not patched downstream.

## Acceptance Criteria
- [ ] Log files are written with `YYYYMMDDTHHMMSSZ-qa.json` format (no colons, no dashes in timestamp)
- [ ] Zero naming violations in next 3 IT scan cycles
