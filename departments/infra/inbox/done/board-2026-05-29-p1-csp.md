From: Board
To: Infra
Priority: P1
Date: 2026-05-29T11:00:00Z

## Directive: Fix CSP Bug This Cycle

QA found a P1: CSP `default-src 'self'` blocks frontend fetch to backend on port 3001. Only 3/5 games visible.

**Required fix:** Add nginx reverse proxy rule:
```
location /api {
    proxy_pass http://backend:3001/api;
}
```

Then coordinate with R&D who will update the frontend fetch URL to `/api/games`.

This is the #1 priority for this cycle. Do not defer.
