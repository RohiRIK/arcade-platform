# Security Audit — 2026-05-30 Cycle 3

## 1. Dependency Audit

`npm audit --json` on `backend/`: **0 vulnerabilities** across 71 production dependencies.
express@4.21.x, cors@2.8.5 — no known CVEs.

**Status**: CLEAN ✓

## 2. Nginx Security Headers

All 5 headers present in `frontend/nginx.conf` (listening on 8080):
- `Content-Security-Policy`: default-src 'self'; script-src 'self' 'unsafe-inline'; style-src 'self' 'unsafe-inline'; img-src 'self' data:; connect-src 'self'
- `X-Frame-Options`: SAMEORIGIN
- `X-Content-Type-Options`: nosniff
- `Referrer-Policy`: strict-origin-when-cross-origin
- `Permissions-Policy`: camera=(), microphone=(), geolocation=()

**Status**: CLEAN ✓

Note: `unsafe-inline` for script-src/style-src accepted for this internal platform (inline scripts in index.html).

## 3. Docker Container Permissions

- **Backend** (`arcade-platform-backend:latest`, 138MB): Runs as non-root `app` user. **CLEAN ✓**
- **Frontend** (`arcade-platform-frontend:latest`, 54MB): Runs as non-root `nginx` user via nginx-unprivileged:alpine, port 8080. **FIXED ✓** (DevOps hardened cycle 4)
- Old `arcade-frontend:latest` (62.4MB) still present — stale image from before hardening. No security risk but should be pruned.

## 4. Backend Code Review — P2 Verification

### PUT /api/config — RESOLVED ✓
`backend/src/index.js` line 34: endpoint now returns `403 Forbidden` with error message. `Object.assign` + `writeFileSync` code removed. Confirmed via source review.

### CORS — RESOLVED ✓
`backend/src/index.js` line 14: `cors({ origin: false })` — rejects all cross-origin requests. All API access via nginx reverse proxy on same origin.

### Path traversal (log reader) — LOW RISK, UNCHANGED
`backend/src/routes/logs.js`: mitigated by `.endsWith('.json')` filter. No user-controlled path segments.

## 5. File Permissions

No world-writable files. No `.env` files. No plaintext secrets. `config.json` contains no credentials.

## Summary

| Finding | Severity | Status |
|---------|----------|--------|
| npm audit | — | Clean ✓ |
| Nginx security headers | — | Clean ✓ |
| Backend Docker root | — | Fixed ✓ |
| Frontend Docker root | P3 | Fixed ✓ (DevOps) |
| PUT /api/config unauth | P2 | Fixed ✓ (R&D) |
| Open CORS | P2 | Fixed ✓ (R&D) |
| unsafe-inline in CSP | Info | Accepted |
| Stale Docker image | Info | Cleanup recommended |

**All previously open findings now resolved.** Zero open vulnerabilities. Next scan in 6 hours.
