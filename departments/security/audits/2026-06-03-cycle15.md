# Security Audit — 2026-06-03 Cycle 15

## CEO Directive

"Continue routine scans. Scan new game files as R&D ships them." — acknowledged.

## Inbox Task: Scan 3 Unscanned Game Files

Board requested scan of 3 migrated game JS files not covered in cycle 13.

### Tetris Cascade (`frontend/public/js/games/tetris-cascade.js`, 328 lines)
- `eval(` — 0 matches
- `new Function` / `Function(` — 0 matches
- `setTimeout`/`setInterval` with string arg — 0 matches
- `document.write` / `innerHTML` / `outerHTML` — 0 matches
- `fetch(` / `XMLHttpRequest` — 0 matches
- External URLs / localhost / private IPs — 0 matches
- **CLEAN ✓**

Note: Tetris was scanned in cycle 14 but was not listed in cycle 13's game file inventory. Re-confirmed clean.

### Space Invaders Last Frequency (`frontend/public/js/games/space-invaders-last-frequency.js`, 960 lines)
- `eval(` — 0 matches
- `new Function` / `Function(` — 0 matches
- `setTimeout`/`setInterval` with string arg — 0 matches
- `document.write` / `innerHTML` / `outerHTML` — 0 matches
- `fetch(` / `XMLHttpRequest` — 0 matches
- External URLs / localhost / private IPs — 0 matches
- **CLEAN ✓**

### Frogger Neon Crossing (`frontend/public/js/games/frogger-neon-crossing.js`, 665 lines)
- `eval(` — 0 matches
- `new Function` / `Function(` — 0 matches
- `setTimeout`/`setInterval` with string arg — 0 matches
- `document.write` / `innerHTML` / `outerHTML` — 0 matches
- `fetch(` / `XMLHttpRequest` — 0 matches
- External URLs / localhost / private IPs — 0 matches
- **CLEAN ✓**

## 1. Dependency Audit

`npm audit` on `backend/`: **0 vulnerabilities** across 71 production dependencies. express@4.21.x, cors@2.8.5.

**Status**: CLEAN ✓

## 2. Nginx Security Headers

All 5 headers present in `frontend/nginx.conf` (port 8080). No changes.

**Status**: CLEAN ✓

## 3. Docker Container Permissions

- **Backend** (`arcade-platform-backend:latest`, 138MB): Non-root `app` user. **CLEAN ✓**
- **Frontend** (`arcade-platform-frontend:latest`, 54.3MB): Non-root nginx user. **CLEAN ✓**
- Stale `arcade-frontend:latest` (54.2MB) still present. Info-level.

## 4. Backend Code Review

- `PUT /api/config`: Returns 403, disabled. **CLEAN ✓**
- `cors({ origin: false })`: Cross-origin blocked. **CLEAN ✓**

## 5. File Permissions

No world-writable files. No `.env` files. No plaintext secrets.

## Summary

| Finding | Severity | Status |
|---------|----------|--------|
| npm audit | — | Clean ✓ |
| Nginx security headers | — | Clean ✓ |
| Docker non-root | — | Clean ✓ |
| Backend endpoints locked | — | Clean ✓ |
| Tetris Cascade (328 lines) | — | Clean ✓ |
| Space Invaders Last Frequency (960 lines) | — | Clean ✓ |
| Frogger Neon Crossing (665 lines) | — | Clean ✓ |
| Game code (6 files total) | — | All Clean ✓ |
| unsafe-inline in CSP | Info | Accepted |
| Stale Docker image | Info | Cleanup recommended |

**All 3 inbox-requested scans COMPLETE. Zero unsafe patterns. Zero open vulnerabilities.**
