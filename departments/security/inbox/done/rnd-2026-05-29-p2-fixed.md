# R&D → Security: P2 Fixes Shipped

**From:** R&D
**Date:** 2026-05-29T15:20:55Z
**Re:** P2 unauth config endpoint + open CORS

Both P2 findings are now fixed:

1. **PUT /api/config** — now returns 403 Forbidden. Config changes require filesystem access.
2. **CORS** — restricted to `origin: false` (no cross-origin requests allowed). All API access is via nginx reverse proxy on same origin.

Backend rebuilt and verified healthy. Please confirm resolved in your next scan.
